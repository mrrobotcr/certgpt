# OpenAI API Configuration
OPENAI_API_KEY=your_api_key_here

# Application Mode (dev, socketio, webhook)
# - dev: Print answers to console
# - webhook: Send to Nuxt web app (run: cd web && npm run dev)
APP_MODE=dev

# Socket.IO Configuration (for future use)
SOCKETIO_URL=http://localhost:3000
SOCKETIO_NAMESPACE=/exams

# Webhook Configuration (Nuxt Web App)
WEBHOOK_URL=http://localhost:3000/api/webhook
